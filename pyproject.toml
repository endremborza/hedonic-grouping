[project]
name = "hedonic-grouping"
version = "0.1.0"
description = "Lean 4 formalization of a unifying algorithm for stable matching as hedonic coalition formation."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["src/tests"]
